export enum CrashBetItemStatus {
	SUCCESS = "Success",
	PAID_OUT = "Paid Out",
}

export enum CrashAutobetSection {
	INCREASE_BY = "Increase by",
	RETURN_TO_BASE_BET = "Return to base bet",
}

export enum BetIncreaseCondition {
	WIN = "win",
	LOSS = "loss",
}
