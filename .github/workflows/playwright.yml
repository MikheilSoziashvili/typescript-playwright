name: Playwright E2E Tests
on:
    workflow_dispatch:
        inputs:
            environment:
                description: "Environment to run tests against"
                type: string
                required: true
            tag:
                description: "Tag scope"
                required: true
                default: "all"
                type: choice
                options:
                    - all
                    - smoke
                    - visual
                    - originals
            custom_options:
                description: "Custom command-line options"
                type: string
                required: false

    workflow_call:
        secrets:
            JIRA_TOKEN:
                required: true
                description: "Jira Token"
            JIRA_USERNAME:
                required: true
                description: "Jira Username"
            XRAY_CLIENT_ID:
                required: true
                description: "X-Ray Client ID"
            XRAY_CLIENT_SECRET:
                required: true
                description: "X-Ray Client Secret"
            SLACK_BOT_USER_OAUTH_TOKEN:
                required: true
                description: "Slack Bot User Oauth Token"
            STEAM_USERNAME:
                required: true
                description: "Steam authentication username"
            STEAM_PASSWORD:
                required: true
                description: "Steam authentication password"
            MAILINATOR_API_KEY: 
                required: true
                description: "Mailinator authentication"
            AWS_ACCESS_KEY_ID:
                required: true
                description: AWS Access key ID
            AWS_SECRET_ACCESS_KEY:
                required: true
                description: AWS Secret Access key
    
    schedule:
        - cron: '0 2 * * *'            

jobs:
    tests:
        timeout-minutes: 60
        runs-on: ubuntu-latest
        env:
            CI: true
            BUILD_NUMBER: ${{ github.run_number }}
            BUILD_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}
            JIRA_USERNAME: ${{ secrets.JIRA_USERNAME }}
            XRAY_CLIENT_ID: ${{ secrets.XRAY_CLIENT_ID }}
            XRAY_CLIENT_SECRET: ${{ secrets.XRAY_CLIENT_SECRET }}
            STEAM_USERNAME: ${{ secrets.STEAM_USERNAME }}
            STEAM_PASSWORD: ${{ secrets.STEAM_PASSWORD }}
            MAILINATOR_API_KEY: ${{ secrets.MAILINATOR_API_KEY }}

            ENVIRONMENT_URL: ${{ inputs.environment == '' && vars.ENVIRONMENT_URL || inputs.environment }}
            SLACK_BOT_USER_OAUTH_TOKEN: ${{ secrets.SLACK_BOT_USER_OAUTH_TOKEN }}
            TAG: ${{ inputs.tag == '' && 'smoke' || inputs.tag }}
        steps:
            - name: Validation For Environment URL
              run: |
                  echo "Validating that Environment URL is valid format (and does not end with slash)"
                  echo $ENVIRONMENT_URL | grep -E '^(http|https)://[a-zA-Z0-9./?=_-]*[^/]$'
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 20
            - name: Install dependencies
              run: yarn install --immutable --immutable-cache --check-cache
            - name: Install Playwright Browsers
              run: yarn playwright install --with-deps
            - name: Run Playwright tests
              run: yarn playwright test ${{ inputs.tag != 'all' && '--grep "$TAG"' || inputs.custom_options }}
            - uses: actions/upload-artifact@v3
              if: always()
              with:
                  name: playwright-report
                  path: playwright-report/
                  retention-days: 30
            - name: Upload Playwright report to S3 and generate presigned URL
              if: always()
              run: |
                # Create a unique directory name using the GitHub run ID and timestamp
                TIMESTAMP=$(date +%Y%m%d%H%M%S)
                S3_KEY_PREFIX="playwright-reports/${{ github.run_id }}-${TIMESTAMP}"
 
                # Call the upload script with the required parameters
                ./scripts/upload_to_s3_and_generate_presigned_url.sh playwright-report gamdom-prod-qa-reporting $S3_KEY_PREFIX
              env:
                AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
                AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                AWS_DEFAULT_REGION: eu-west-2
            - name: Create PR comment with presigned URL
              if: github.event_name == 'pull_request'
              continue-on-error: true
              run: |
                COMMENT=":world_map: Playwright report available [here]($PRESIGNED_URL)"
                echo $COMMENT
                gh pr comment ${{ github.event.pull_request.number }} --body "$COMMENT"
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}